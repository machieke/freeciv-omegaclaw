# FDAS PR101 retained-capacity decision-safe readout feasibility

Date: 2026-08-06

## Result

The mechanics audit is accepted and the progression decision is `not-ready`.
The exact PR99 model and accepted PR100 confirmation were loaded without
refitting.  Two complete audits are byte-identical with structural hash
`e910e975ee07ea233965daa40d5cf01ffe2d7c93e9dfa3ea155160e2a104b6a5`
and report SHA-256
`932939db67520b2488eb0f0e5e23653eb603c3d8c87cf53769f01e35e1af29e3`.

The frozen model contains six hierarchy/signature groups with numerically
usable intervals for both exact-product effect and durable goal relief.  Their
30 possible ordered comparisons contain:

- zero strictly separated durable-relief interval pairs;
- zero pairs satisfying the full relief-separation plus product
  non-inferiority rule; and
- no prospectively recorded and independently confirmed nonselected-candidate
  outcome population.

The result is stronger than a low-yield live diagnostic: under the frozen
intervals, no categorical candidate pairing can pass the preregistered strict
readout rule even before an engine state is considered.  Starting a gameplay
cohort would therefore be incapable of producing an eligible readout without
changing the model, the safety rule, or the evidence population after seeing
the result.

## Implemented boundary

PR101 adds a typed protected readout component for future evidence.  It:

- constructs the exact PR95 feature vocabulary from a current, byte-identical
  retained production queue;
- binds each query to its current snapshot, dependent revision, deficit atom,
  grounded production assembly, candidate hash, and scalar rank;
- exposes `scalar-selected` and `nonselected-unvalidated` roles;
- requires numerical product and relief estimates for every candidate;
- requires strict durable-relief interval dominance and exact-product
  lower-bound non-inferiority;
- deterministically resolves only candidates passing both conditions; and
- explicitly abstains while nonselected outcomes remain unconfirmed.

The implementation never changes the candidate surface, scalar pressure
selection, or action selection.  It grants no readout, policy, truth, or action
authority.  The original proposal/outcome query builder and the candidate
readout query builder share one feature constructor, preventing silent schema
drift.

## Scientific conclusion

PR100 confirmed calibration for PF-selected retained queues, but that evidence
is insufficient for policy-divergent candidate readout.  The confirmed
intervals overlap completely, and selected-only calibration cannot be silently
transported to alternatives.  This closes the immediate readout and engine
gate under the plan's stop rule.

A future track must begin with a new preregistration that observes complete
current retained-queue candidate surfaces, records selection role and scalar
rank prospectively, and follows selected and nonselected already-authoritative
queues to exact product and durable-relief outcomes.  Only a disjoint
confirmation showing nonselected coverage and genuinely separable intervals
could reopen shadow readout evaluation.

## Verification

- frozen audit: accepted, deterministic, `not-ready`;
- joint usable target bins: `6`;
- ordered pair checks: `30`;
- strict relief separations: `0`;
- decision-safe interval pairs: `0`;
- nonselected outcome confirmation: `false`;
- two audit outputs: byte-identical;
- complete FDAS regression suite: `595 passed`;
- focused retained-capacity, transition-model, confirmation, feature-builder,
  and unit-defense tests are included in that clean aggregate.

The machine-readable report is
`fdas-pr101-retained-capacity-decision-safe-readout.json`.
